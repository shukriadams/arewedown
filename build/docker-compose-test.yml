version: "3.5"
services:
  arewedowntest:
    image: shukriadams/arewedown:latest
    container_name: arewedowntest
    restart: unless-stopped
    ports:
    - "7018:3000"

networks:
   default:
      external: true
      name: testingNetwork    