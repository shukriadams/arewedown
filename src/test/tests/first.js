let assert = require('madscience-node-assert'),
    test = require('./../helpers/testBase');

test('invalid input', function(testBase){

    it('should just work', async function(done) {
        done();
    });
});